## Header

- `Source audit`: `\\?\C:\dev\rust_agent\.audit\audit-1781906077.md` (validated locally via `C:\dev\rust_agent\.audit\audit-1781906077.md`)
- `Planning date`: `2026-06-19`
- `Target workspace`: `C:\dev\rust_agent`
- `Planning mode`: `plan only, no code changes`
- `Confidence`: `medium` because the audit’s main architectural findings still match the workspace, but its dirty-worktree snapshot is stale and the workflow redesign path depends on one material execution-boundary decision.

## 1. Audit Digest

- Scope audited: the single-crate Rust workspace rooted at `C:\dev\rust_agent`, especially [`crates/app/src/main.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/main.rs), [`crates/app/src/automate.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/automate.rs), [`crates/app/src/plan.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/plan.rs), [`crates/app/src/review.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/review.rs), [`crates/app/src/implementation_audit.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/implementation_audit.rs), [`crates/app/src/fix_loop.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/fix_loop.rs), [`crates/app/src/artifact.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/artifact.rs), and [`workflows/refactor.json`](/abs/path/C:/dev/rust_agent/workflows/refactor.json).
- Architecture snapshot: one binary crate (`app`) orchestrates Codex-driven workflows. `main.rs` still owns top-level dispatch plus most parse/run branches; `automate.rs` shells back into the current executable, scrapes stdout for artifact paths, and reads audit Markdown text for clean-stop detection; the per-command modules mainly encapsulate prompt construction, path resolution, and artifact saving.
- Findings by severity:
  - High: `main.rs` remains a command god-module with repeated parse and run paths.
  - High: workflow orchestration remains brittle because `automate.rs` depends on human-facing stdout text and Markdown clean markers.
  - Medium: subcommand knowledge is duplicated between `main.rs`, `automate.rs`, help output, and workflow assumptions.
  - Medium: boundary modules are still thin wrappers and do not yet own end-to-end command lifecycles.
  - Medium: test coverage remains strongest for parsing/string assembly, weakest at the workflow/process boundary.
- Quick wins preserved from the audit:
  - Centralize subcommand metadata.
  - Add a stable machine-readable result channel for workflow steps.
  - Add one workflow contract test that proves artifact chaining without depending on localized phrasing.
  - Move command lifecycle ownership out of `main.rs`.
- Roadmap preserved from the audit:
  - Stabilize boundary tests and result contracts before structural extraction.
  - Simplify by consolidating command metadata and module ownership.
  - Reshape workflow execution only after contracts and tests exist.
- Open questions and validation gaps:
  - Whether recursive self-process execution is required for isolation or is only a shortcut.
  - Whether external consumers depend on the current human-readable stdout phrases.
  - Whether loop-stop semantics should remain marker-based or become explicit status-based.
  - The audit’s recorded dirty state is stale.

## 2. Freshness Check

- Planning used both the audit and targeted workspace inspection.
- Referenced files or modules checked:
  - [`Cargo.toml`](/abs/path/C:/dev/rust_agent/Cargo.toml)
  - [`crates/app/Cargo.toml`](/abs/path/C:/dev/rust_agent/crates/app/Cargo.toml)
  - [`crates/app/src/main.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/main.rs)
  - [`crates/app/src/automate.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/automate.rs)
  - [`crates/app/src/plan.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/plan.rs)
  - [`crates/app/src/review.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/review.rs)
  - [`crates/app/src/implementation_audit.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/implementation_audit.rs)
  - [`crates/app/src/fix_loop.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/fix_loop.rs)
  - [`crates/app/src/artifact.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/artifact.rs)
  - [`workflows/refactor.json`](/abs/path/C:/dev/rust_agent/workflows/refactor.json)
- Referenced files or modules not found: none from the audit’s cited set.
- Commands or manifests checked:
  - `git status --short`
  - `cargo test`
  - `rg --files crates/app/src workflows`
  - `rg -n ... crates/app/src/main.rs crates/app/src/automate.rs`
- Audit claims needing revalidation before coding:
  - Dirty worktree: audit reported modified `crates/app/src/automate.rs` and `workflows/refactor.json`; current `git status --short` shows only untracked `.audit/audit-1781906077.md`.
  - Any assumption that workflow brittleness is currently being addressed in-flight is now unsubstantiated.
- Local changes or missing repository context affecting the plan:
  - The newly created audit file is untracked and should be treated as input only.
  - `cargo test` currently passes with `57` tests, which gives a stable baseline but does not cover external-process workflow execution.

## 3. Planning Assumptions

- `Safe`: The repository remains a single-member workspace with one binary crate, so command-registry and command-module extraction can stay lightweight.
- `Safe`: The implementation should preserve current user-facing help text and final artifact-save messages unless a decision gate explicitly authorizes format changes.
- `Decision gate`: Direct in-process execution of service commands from `automate.rs` is acceptable if it preserves behavior and does not violate an intended isolation contract.
- `Decision gate`: A machine-readable result side channel may be added without breaking external consumers of stdout.
- `Validation needed`: Focused workflow tests may require introducing an injectable step runner seam rather than only black-box process-spawn tests.
- `Validation needed`: The implementation agent should confirm whether additional docs or wrapper scripts consume strings like `"Plan enregistre dans"` or clean markers from `workflows/refactor.json`.

## 4. Finding Traceability

| Audit finding | Severity | Disposition | Task IDs / gate | Notes |
| --- | --- | --- | --- | --- |
| `main.rs` is a command god-module | High | Planned | `P2-T1`, `P2-T2`, `P3-T1`, `P3-T2` | Reduce `main.rs` to dispatch and shared glue only. |
| Workflow orchestration depends on stdout scraping and Markdown markers | High | Planned | `P1-T1`, `P1-T2`, `P4-T1`, `P4-T2`, `G1`, `G2`, `G3` | Stabilize tests first, then replace brittle boundaries. |
| Command knowledge duplicated across switchboards | Medium | Planned | `P2-T1`, `P2-T2` | Central registry should feed parse/dispatch/help/automation checks. |
| Boundary modules are thin wrappers | Medium | Planned | `P3-T1`, `P3-T2` | Move parse/validate/run/save ownership into command modules. |
| Test coverage weak at process/workflow boundary | Medium | Planned | `P1-T1`, `P1-T2`, `P4-T1` | Add contract tests before risky reshaping. |
| Audit dirty-worktree snapshot is stale | Low | Already resolved | `G4` | Revalidate before coding; no current evidence of in-flight overlapping edits. |

## 5. Recommended Sequence

Sequence the work from lowest-risk contract hardening to higher-risk orchestration changes: first add the missing workflow boundary tests and a narrow test seam, then centralize command metadata, then extract lifecycle ownership out of `main.rs`, and only then replace stdout/Markdown coupling with structured results. Treat direct in-process workflow execution as a gated final step, not the opening move.

### Phase 1: Stabilize Workflow Contracts

Objective: make workflow/process behavior testable before structural refactors.

Dependencies: none.

Expected outcome: reproducible tests cover artifact chaining, failure propagation, and clean-stop semantics without requiring live Codex calls.

Tasks: `P1-T1`, `P1-T2`

### Phase 2: Centralize Command Knowledge

Objective: eliminate drift across parse/dispatch/help/automation command metadata.

Dependencies: Phase 1 complete.

Expected outcome: one authoritative registry defines subcommands, aliases, codex-option capability, and help-facing metadata.

Tasks: `P2-T1`, `P2-T2`

### Phase 3: Move Lifecycle Ownership Out of `main.rs`

Objective: give each service command module end-to-end responsibility for parse/validate/run/save.

Dependencies: Phase 2 complete.

Expected outcome: `main.rs` becomes a thin dispatcher; command modules carry their own lifecycle logic.

Tasks: `P3-T1`, `P3-T2`

### Phase 4: Replace Brittle Workflow Result Channels

Objective: stop internal automation from depending on human-facing stdout phrases and audit Markdown markers.

Dependencies: Phases 1-3 complete, `G1` and `G2` resolved or defaulted, `G3` default applied.

Expected outcome: internal workflow execution consumes structured outcomes; human output remains a presentation layer.

Tasks: `P4-T1`, `P4-T2`

## 6. Task Backlog

#### P1-T1: Add focused workflow contract tests for artifact chaining and loop-stop behavior

- Priority: High
- Source finding: Workflow brittleness; weak workflow/process-boundary test coverage
- Rationale: The refactor is unsafe until the current workflow contract is pinned down by tests.
- Target files/modules: [`crates/app/src/automate.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/automate.rs), existing `#[cfg(test)]` block; optionally a new `tests/` integration target only if a private seam is insufficient
- Dependencies: none
- Implementation outline:
  1. Add tests covering artifact propagation across steps, step failure propagation, and clean-stop evaluation against `loop_policy.audit_step`.
  2. Prefer exercising `run_workflow` behavior through simulated step outputs rather than duplicating logic in helper-only tests.
  3. Preserve existing unit tests such as `extracts_artifact_path_from_command_output` and extend them only where they support the contract.
- Tests to add or update: workflow-step artifact chaining; clean stop when audit step artifact contains a clean signal; failure stops workflow and reports step name/status
- Verification commands: `cargo test`
- Acceptance criteria:
  - `automate.rs` contains tests that fail if artifact chaining or loop-stop semantics regress.
  - The tests do not require live Codex network execution.
- Rollback/fallback notes: If black-box `run_workflow` tests prove too brittle immediately, land helper-level tests first and pair them with `P1-T2` in the same branch.
- Notes for implementation agent: Do not lock tests to localized French prose unless the point of the test is proving current compatibility.

#### P1-T2: Introduce a narrow execution seam for workflow testing

- Priority: High
- Source finding: Workflow orchestration is process-coupled and hard to test safely
- Rationale: `run_workflow` currently shells into `env::current_exe()`, which blocks realistic contract tests without either fragile subprocess fixtures or a small injected seam.
- Target files/modules: [`crates/app/src/automate.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/automate.rs)
- Dependencies: `P1-T1`
- Implementation outline:
  1. Extract step execution behind a private helper boundary, such as an internal runner function or test-only injectable executor.
  2. Keep production behavior identical: same executable, same args, same stdout/stderr capture.
  3. Use the seam only to simulate outputs in tests; do not introduce a public abstraction layer unless needed later.
- Tests to add or update: end-to-end-like `run_workflow` tests using fake step outputs
- Verification commands: `cargo test`
- Acceptance criteria:
  - Workflow tests can simulate multiple steps and failures without spawning the real binary.
  - Production code path still uses the current executable in non-test builds.
- Rollback/fallback notes: If a private injectable seam becomes awkward, use a `cfg(test)` helper rather than a permanent public trait.
- Notes for implementation agent: Keep this seam intentionally small; this is a safety tool, not a framework.

#### P2-T1: Create a single authoritative subcommand registry

- Priority: High
- Source finding: Duplicated command knowledge across `main.rs`, `automate.rs`, help output, and workflow assumptions
- Rationale: Central metadata reduces compiler-invisible drift and simplifies later module extraction.
- Target files/modules: new `crates/app/src/command_registry.rs` or equivalent; [`crates/app/src/main.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/main.rs); [`crates/app/src/automate.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/automate.rs)
- Dependencies: `P1-T1`, `P1-T2`
- Implementation outline:
  1. Define a compact registry for subcommand identifiers, aliases, and automation capabilities.
  2. Move `known_subcommand`, `command_accepts_codex_options`, and alias recognition logic to the registry.
  3. Keep `automate.rs` and `main.rs` consumers thin by asking the registry instead of repeating matches.
- Tests to add or update: parse alias coverage; registry-driven automation capability checks; existing parse tests affected by alias lookup changes
- Verification commands: `cargo test`
- Acceptance criteria:
  - Adding or renaming a subcommand requires editing one metadata source, not multiple match blocks.
  - `automate.rs` no longer carries its own hard-coded command list separate from CLI dispatch.
- Rollback/fallback notes: If help text cannot yet be fully generated from the registry, still centralize capability and alias knowledge first.
- Notes for implementation agent: Do not introduce a heavy CLI framework; the audit explicitly does not justify that level of abstraction.

#### P2-T2: Derive help and top-level dispatch checks from the registry

- Priority: Medium
- Source finding: `main.rs` still duplicates parse and help concerns
- Rationale: Once metadata is centralized, help output and top-level recognition should stop being manually mirrored.
- Target files/modules: [`crates/app/src/main.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/main.rs); registry module from `P2-T1`
- Dependencies: `P2-T1`
- Implementation outline:
  1. Refactor top-level subcommand detection in `parse_args` to consult the registry.
  2. Refactor `print_help` to use registry-backed command descriptors where practical.
  3. Preserve the current visible command surface and examples unless a clear bug is being corrected.
- Tests to add or update: top-level parse tests; help smoke test if one is added
- Verification commands: `cargo test`, `cargo run -q -p app -- --help`
- Acceptance criteria:
  - Subcommand recognition and help coverage are driven by the same command source.
  - Help output still lists the current supported commands and aliases.
- Rollback/fallback notes: If full help generation adds too much churn, keep example text static but source the command list and aliases from the registry.
- Notes for implementation agent: Treat visible help text as compatibility-sensitive.

#### P3-T1: Extract command lifecycle ownership into per-command modules

- Priority: High
- Source finding: `main.rs` is a god-module; boundary modules are thin wrappers
- Rationale: The per-command modules should own parse, validate, prompt build, execution, and artifact-save behavior to reduce coupling and change blast radius.
- Target files/modules: [`crates/app/src/main.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/main.rs); [`crates/app/src/plan.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/plan.rs); [`crates/app/src/review.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/review.rs); [`crates/app/src/implementation_audit.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/implementation_audit.rs); [`crates/app/src/fix_loop.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/fix_loop.rs); likely a new `crates/app/src/audit.rs`
- Dependencies: `P2-T1`, `P2-T2`
- Implementation outline:
  1. Move each command’s parse and run logic behind module-level entry points.
  2. Create an `audit` module so audit follows the same ownership model as plan/review/fix-loop.
  3. Leave `main.rs` responsible only for argument collection, dispatch selection, and process exit behavior.
- Tests to add or update: existing parse tests in `main.rs`; module-specific tests for prompt/run helpers where logic moves
- Verification commands: `cargo test`
- Acceptance criteria:
  - `main.rs` no longer contains most of the per-command `parse_*` and `run_*` lifecycle logic.
  - Each service command module can be read independently to understand its end-to-end behavior.
- Rollback/fallback notes: If full extraction is too large for one pass, move one command family at a time, starting with `audit` and `plan`.
- Notes for implementation agent: Preserve unrelated prompt wording and artifact naming while relocating ownership.

#### P3-T2: Consolidate repeated Codex final-message handling into shared command execution helpers

- Priority: Medium
- Source finding: repeated `run_*` branches duplicate final-message, nonzero-status, and artifact-save handling
- Rationale: This is low-risk duplication removal that supports phase 3 without forcing a broad redesign.
- Target files/modules: [`crates/app/src/main.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/main.rs); command modules touched in `P3-T1`; possibly a new internal helper module
- Dependencies: `P3-T1`
- Implementation outline:
  1. Extract shared “run Codex until final message, preserve final artifact even on nonzero exit” behavior into a reusable helper.
  2. Parameterize only what actually varies: status banner, missing-message error text, artifact saver, and success label.
  3. Keep command-specific prompts and path validation in their respective modules.
- Tests to add or update: command-run behavior tests where practical; keep `cargo test` green
- Verification commands: `cargo test`
- Acceptance criteria:
  - Repeated final-message/status handling logic is reduced materially.
  - Command-specific behavior remains readable and does not become callback-heavy.
- Rollback/fallback notes: If a generic helper starts obscuring behavior, prefer two small helpers over one over-abstracted executor.
- Notes for implementation agent: Optimize for clarity, not maximum deduplication.

#### P4-T1: Introduce a structured internal command outcome for service commands

- Priority: High
- Source finding: workflow orchestration depends on stdout parsing and artifact-save phrasing
- Rationale: Automation needs a stable machine-readable result boundary separate from presentation text.
- Target files/modules: command modules from `P3-T1`; [`crates/app/src/automate.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/automate.rs); possibly a new shared `command_result.rs`
- Dependencies: `P3-T1`, `P3-T2`, `G2`
- Implementation outline:
  1. Define an internal outcome structure for service commands with at least command name, exit status, final message presence, and artifact path.
  2. Have internal command handlers return this structure while preserving current terminal output for humans.
  3. Update workflow execution to consume the structured artifact path rather than scraping `" enregistre dans "`.
- Tests to add or update: structured outcome tests; workflow tests proving artifact chaining no longer depends on human phrasing
- Verification commands: `cargo test`
- Acceptance criteria:
  - Internal automation can locate produced artifacts without parsing human output lines.
  - User-facing stdout remains compatible unless an explicit compatibility decision says otherwise.
- Rollback/fallback notes: If stdout compatibility is sensitive, add the structured outcome only for in-process consumers first and leave printed lines unchanged.
- Notes for implementation agent: Treat machine-readable outcomes as internal API; name and shape them accordingly.

#### P4-T2: Replace marker-based workflow loop-stop logic with explicit audit status, then optionally eliminate recursive self-spawn

- Priority: High
- Source finding: clean-stop semantics depend on reading audit Markdown; workflow still respawns the current executable
- Rationale: Marker-based stop rules are brittle, and direct internal execution only becomes attractive after result contracts exist.
- Target files/modules: [`crates/app/src/automate.rs`](/abs/path/C:/dev/rust_agent/crates/app/src/automate.rs); command modules; [`workflows/refactor.json`](/abs/path/C:/dev/rust_agent/workflows/refactor.json)
- Dependencies: `P4-T1`, `G1`, `G3`
- Implementation outline:
  1. Add an explicit clean/not-clean status to the relevant audit-alignment command outcome so loop termination does not depend on Markdown phrases.
  2. Update `run_workflow` to use explicit status as the primary stop signal, with marker fallback only if compatibility requires a transitional period.
  3. If `G1` is approved, replace recursive self-process execution for service commands with direct handler invocation; keep direct-run exec prompts on the existing path.
- Tests to add or update: loop termination contract tests; compatibility test for transitional marker fallback if retained
- Verification commands: `cargo test`
- Acceptance criteria:
  - Clean-stop decisions are driven by explicit status, not report prose.
  - Service-command workflow execution no longer needs stdout scraping; self-spawn is removed only if the isolation gate allows it.
- Rollback/fallback notes: If direct invocation proves risky, keep subprocess execution but still consume a structured side channel rather than stdout text.
- Notes for implementation agent: This is the highest-risk slice. Land it only after earlier phases are green and small.

## 7. Verification Matrix

| Area | Command or check | Provenance | Expected result | Covers tasks | Notes |
| --- | --- | --- | --- | --- | --- |
| Baseline regression suite | `cargo test` | Observed | All tests pass; baseline currently `57 passed, 0 failed` | All tasks | Primary merge gate for this repo today. |
| Command help surface | `cargo run -q -p app -- --help` | Source inferred | Help renders successfully and still lists supported commands/aliases | `P2-T2`, `P3-T1` | No network/Codex execution required. |
| Workflow contract tests | `cargo test automate::tests` | Source inferred | New workflow boundary tests pass | `P1-T1`, `P1-T2`, `P4-T1`, `P4-T2` | Tight feedback loop while refactoring `automate.rs`. |
| Parse/dispatch regressions | `cargo test tests::parses_` | Source inferred | Existing top-level parse tests remain green | `P2-T1`, `P2-T2`, `P3-T1` | Use exact test filters available after implementation. |
| Command-module prompt/path tests | `cargo test plan::tests review::tests implementation_audit::tests fix_loop::tests` | Source inferred | Module tests stay green after extraction | `P3-T1`, `P3-T2` | Filter syntax may need adjustment to actual harness naming. |
| Worktree sanity before coding | `git status --short` | Observed | No unexpected overlapping edits beyond planned implementation work | Pre-flight for all tasks | Current state shows only untracked audit input. |
| External Codex integration | Manual smoke check using a non-destructive local command path | Placeholder | Structured outcomes and visible output remain compatible in a real Codex-backed run | `P4-T1`, `P4-T2` | Cannot be completed during planning without real Codex execution and local operator approval of prompt choice. |

## 8. Decision Gates

| Gate | Status | Blocks | Question | Recommended default | How to validate |
| --- | --- | --- | --- | --- | --- |
| `G1` | Defaultable | `P4-T2` direct-invocation portion | Is recursive self-process execution an intentional isolation boundary? | Default to keeping subprocess execution until structured outcomes exist; only remove self-spawn after behavior parity is proven. | Search docs/history for explicit isolation rationale; if absent, compare tests and behavior before/after direct invocation. |
| `G2` | Validation | `P4-T1` | Do any external tools depend on exact stdout phrases like `"Plan enregistre dans"`? | Preserve existing human output and add an internal side channel first. | Search repo and wrapper scripts for those strings before changing visible output semantics. |
| `G3` | Defaultable | `P4-T2` | Should clean-stop semantics be based on audit text markers at all? | Move to explicit status as primary signal; keep temporary marker fallback only if needed for compatibility. | Inspect current workflow consumers and confirm tests cover both new explicit status and fallback removal path. |
| `G4` | Validation | Start of implementation | Is the worktree still limited to the untracked audit input? | Re-run `git status --short` before editing and preserve unrelated user changes. | Validate immediately before the first code edit. |

## 9. Out of Scope

- Migrating the CLI to `clap` or any other framework.
- Rewriting `codex.rs` unless phase work proves an unavoidable coupling point.
- Changing prompt wording for stylistic reasons only.
- Renaming workflow steps or artifact prefixes without a compatibility reason.
- Broad cleanup of unrelated tests, formatting, or module naming.
- Changing model names, workflow defaults, or external service behavior beyond what the refactor requires.
- Modifying the untracked audit artifact except to read it as input.

## 10. Implementation Agent Prompt

Implement the plan above in `C:\dev\rust_agent`, starting with **Phase 1: Stabilize Workflow Contracts**. Preserve unrelated user changes, treat `C:\dev\rust_agent\.audit\audit-1781906077.md` as read-only input, and stay within the planned scope only. Run at least `git status --short`, `cargo test`, and the most relevant focused test commands for the modules you touch before finishing. Code changes are authorized for the implementation pass, but do not introduce unrelated cleanup or framework churn.